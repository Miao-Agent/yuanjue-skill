---
name: "META-元决"
description: "元决 v2.0 — 指令自优化引擎。S0-S6管线：内容分析→问心沟通→搜集扫描→化神分析(动态轮数)→输出优化→自升级。元层面的决断权。"
tags: ["META", "核心引擎", "自优化", "元决"]
platform: windows
version: "2.0"
source: SOUL v8.7.3 → 天工v3.0优化
---

# 元决 v2.0 — 指令自优化引擎

## 触发词
元决: {指令} / 请优化此条指令 / 5条件自触发

## 概述
接收原始指令→S0-S6管线→化神评分(动态轮数)→输出优化后指令。深度融合问心多轮沟通+天工扫描+化神分析。

## 5条件自触发简化模式
以下任一自动进入元决模式:
1. 指令含模糊词(随便/差不多/你看着办/帮我弄个/搞一下)
2. 指令目标缺失(缺少动词/缺少产出物)
3. 指令与上下文冲突
4. 指令跨域无指定优先级
5. 指令超50字无分段

---

## S0-S6 全流程管线

```
用户输入文本
  │
  ├─ S0 内容分析 ──── 完整度评估
  │   ├─ ≥7.0 → 直接 S2
  │   └─ <7.0 → S1 问心
  │
  ├─ S1 问心沟通 ──── 多轮信息补充
  │   ├─ 自动识别信息缺口
  │   ├─ 给出扫描策略 + 获取用户确认
  │   └─ 确认扫描范围
  │
  ├─ S2 域识别+角色匹配 ──── 路由决策
  │   ├─ 判定域(DEV/WRITE/ART/MEDIA/...)
  │   ├─ 角色智库匹配专家
  │   └─ 确定扫描策略
  │
  ├─ S3 搜集扫描 ──── 按需采集
  │   ├─ 按域+范围扫描对应区域
  │   ├─ ART域: 额外扫描colors_and_type.css + assets/audio/manifest.json + assets/video/*.webm
  │   ├─ MEDIA域: 额外扫描assets/audio/*.wav + page_audio/*.wav
  │   ├─ **粹池匹配**: 扫描 `_粹池启动词映射表.md` → 按域筛选 → 按触发词匹配 → 多skill装配(单/并行/管线/跨域)
  │   ├─ 提取可利用内容
  │   └─ 整理待分析材料
  │
  ├─ S4 化神分析 ──── 动态轮数评分
  │   ├─ 维度: 可利用度/关联度/完整性/一致性/价值度
  │   ├─ 轮数: 动态(简单3轮/中等6轮/复杂9+轮)
  │   └─ 每轮评分+收敛判断
  │
  ├─ S5 输出优化 ──── 结果交付
  │   ├─ 优化后指令词
  │   ├─ 优化日志（含每轮记录）
  │   ├─ 写入开工指令集
  │   └─ (ART动效) 输出动效开工包: colors_and_type.css + pXX.html + page_audio/ + capture.py
  │
   └─ S6 自升级 ──── 更新本SKILL
      └─ 根据优化经验更新元决自身
```

---

## 多轮沟通 — 功能选择询问 (v3.0增强)

元决入口自动识别用户意图, 弹出功能选择:

### 入口询问
```
用户: "元决 {指令}"
  → S0内容分析自动判定完整度
  → composite≥7.0 → 直接S2
  → composite<7.0 → S1问心:
     "信息缺口识别结果:
      - 目标[明确/模糊]
      - 产出[已定义/缺失]
      - 域[可识别/不明]
      - 参考[有/无]
      是否需要补充以下信息?
      1. 补充目标描述
      2. 定义产出物格式
      3. 指定域/角色
      4. 提供参考上下文
      5. 全部补充"
```

### 熔锻指令优化 (熔锻→元决)
```
用户: "熔锻→元决 {熔锻指令}"
  → 定向S0-S5(熔锻域):
  "熔锻指令优化目标?
   1. 清晰化 — 模糊词补全
   2. 完整化 — 缺参数/路径/产出
   3. 批量优化 — 多条指令
   4. 开工存档 — 存入开工指令集"
```

### 跨域指令优化
```
用户: "元决 {跨域指令}"
  → S2判定跨域 → 弹出多域选择:
  "指令跨以下域: [DEV, WRITE]
   请指定优先级排序:
   1. DEV优先 > WRITE其次
   2. WRITE优先 > DEV其次
   3. 并行优化(不分先后)"
```

## S0 内容分析

**目标**: 评估输入指令的信息完整度，决定是否需要问心补充。

### 完整度评估维度

| 维度 | 权重 | 评分标准(1-10) |
|------|------|---------------|
| 目标明确度 | 30% | 是否有清晰的目标描述 |
| 产出物定义 | 20% | 是否明确要产出什么 |
| 约束条件 | 15% | 是否有边界/限制条件 |
| 域标识 | 15% | 是否能判断归属域 |
| 参考信息 | 10% | 是否提供背景/参考 |
| 优先级 | 10% | 是否指明优先级/紧急度 |

**composite = Σ(维度分 × 权重)**

**决策**:
- composite ≥ 7.0 → 信息充足，跳过S1，直接进入S2
- 5.0 ≤ composite < 7.0 → 轻量问心(1-2轮)，仅补缺失维度
- composite < 5.0 → 完整问心(S1全流程)

### 信息缺口自动识别

根据各维度评分，自动定位缺口:

| 缺口模式 | 识别信号 | 问心策略 |
|---------|---------|---------|
| 目标模糊 | 无动宾结构/含"搞""弄" | 追问具体目标 |
| 产出未定 | 没说要什么 | 问产出类型(指令词/文档/代码/设计) |
| 无边界 | 没有约束/范围 | 问边界条件 |
| 域不明 | 看不出领域 | 让用户描述场景 |
| 无参考 | 没有上下文 | 问背景/是否有参考 |

---

## S1 问心沟通

**目标**: 通过多轮对话补全信息缺口，确认扫描范围。

**载荷**: 问心引擎(THINK-沟通-问心)
**协议**: 每次只问1个最关键的问题，回答后再推进

### 问心流程

```
S0识别的信息缺口 → 按优先级排序 → 逐轮提问
                                  ↓
                          用户回答 → 更新完整度评估
                                  ↓
                          仍有缺口 → 下一轮提问
                                  ↓
                          缺口补齐 → 进入S1.5
```

### S1.5 扫描策略与确认

信息补齐后，元决自动判断需要扫描的范围:

1. **自动路由**: 根据域+指令内容，匹配扫描策略
   ```
   "扫描以下区域获取优化素材:
    [区域1] → 原因
    [区域2] → 原因
    是否执行扫描？是否有其他区域需要补充？"
   ```
2. **用户确认**: 用户确认/调整扫描范围
3. **确认后进入S2**

### 沟通终止条件

| 条件 | 动作 |
|------|------|
| 所有缺口补齐 | 进入S2 |
| 用户说"直接做" | 跳过S1.5，进入S2用默认扫描 |
| 连续3轮无新信息 | 用当前理解推进 |
| 用户说"不扫了" | 跳过S3，直接用已有信息进入S4 |

---

## S2 域识别 + 角色匹配

**目标**: 判定指令归属域，匹配最优专家角色。

### 域识别(复用域路由)

| 关键词特征 | 匹配域 |
|-----------|--------|
| 代码/编程/bug/API/后端/前端/测试/数据库 | DEV |
| 文案/小红书/抖音/公众号/种草/脚本/口播 | WRITE |
| 出图/设计/3D/海报/插画/电商图/品牌/竖屏动效/动效页面 | ART |
| 视频/剪辑/剪映/字幕/配音/短视频/音频合成/混音/BGM | MEDIA |
| PPT/电商/报告/数据分析/竞品/excel | WORK |
| 学习/研究/调研/归档/论文/消化 | STUDY |
| 思维/分析/决策/推演/逻辑 | THINK | 阴符经(无为而治/阴阳互根) / 老子(道法自然/上善若水) |

### 角色匹配(复用角色智库)

| 域 | 默认角色 | 备选角色 |
|----|---------|---------|
| DEV | 全栈工程师 | 架构师/安全工程师 |
| WRITE | 文案审查员 | 内容策略师/传播分析师 |
| ART | 视觉设计师 | 品牌设计师/3D美术师 |
| MEDIA | 视频审查员 | 剪辑师/内容策划 |
| WORK | 数据审查员 | 商业分析师/产品经理 |
| STUDY | 学习审查员 | 研究员/知识管理师 |
| META | 系统架构师 | 指令优化专家 |

---

## S3 搜集扫描

**目标**: 按S2确定的域+范围，扫描对应区域，提取可用于指令优化的内容。

### 扫描策略矩阵

| 域 | 默认扫描区域 | 扫描方法 | 提取目标 |
|----|------------|---------|---------|
| DEV | <指令库>\文字-AI提示词-工程\ + 技能库\DEV-* | 读INDEX→匹配文件 | 代码规范/架构模式/API定义 |
| WRITE | <指令库>\文字-写作-通用\ + 技能库\WRITE-* | 读INDEX→匹配文件 | 写作框架/语气模板/格式规范 |
| ART | <指令库>\图像-* + 技能库\ART-* | 读INDEX→匹配文件 | 设计参数/风格库/提示词模板 |
| MEDIA | 技能库\MEDIA-* | 读INDEX→匹配文件 | 视频参数/分镜模板/剪辑规范 |
| WORK | 技能库\WORK-* | 读INDEX→匹配文件 | 模板规范/分析框架 |
| STUDY | <知识库>\ + 研究报告 + 熔锻报告 | 读INDEX→匹配文件 | 领域知识/参考案例/熔断记录 |
| META | 技能库\META-* + <.trae-rules>\ + 熔锻报告 | 读INDEX→匹配文件 | 系统架构/优化模式/批量处理历史 |
| **熔锻域** | **熔锻启动词 + 桥接引擎 + SKILL.md + 通用-启动词/** | **读启动词→匹配指令类型** | **熔锻指令模板/桥接路由/九路提取参数** |

### 熔锻指令专项扫描 (S3增强)

当元决识别为熔锻指令优化时, 额外扫描:

| 扫描目标 | 内容 | 提取目的 |
|:---------|:-----|:---------|
| 熔锻启动词.md | 触发词+路由+红线 | 匹配指令标准化触发词 |
| 熔锻-Skill桥接.md | 12桥接目标+启动词 | 完善桥接指令 |
| 通用-启动词/熔锻全流程通用指令.md | Step 0-5.5+R1-R6 | 对齐全流程红线 |
| 通用-门控/熔锻三道路径门禁规范.md | 问心/化神/天工 | 补充门禁参数 |
| 粹池启动词映射表 | 熔锻→各域桥接历史 | 避免重复桥接 |

### 扫描流程

```
S2域判定
  │
  ├─ Step 3.1: 按域定位扫描目录
  ├─ Step 3.2: 读目标目录INDEX.md
  ├─ Step 3.3: 匹配与指令相关的内容文件
  ├─ Step 3.4: 提取可用的模式/参数/格式/方法
  └─ Step 3.5: 整理成待分析材料清单
```

### 3.5 方法论注入 (元决强化 2026-08-09)

**目标**: 在扫描基础上，为指令匹配可复用的方法论/工具/结构，而非只复制参数。

1. **知识库匹配**: 按域匹配 模板 / 案例 / 风格标签 / 场景标签 (来源: 案例库+风格库+指令库)
2. **工具指令词能力表** (来源: 图像/15-提示词方法库-02工具指令词):

| 工具 | 能力 | 适用 |
|------|------|------|
| APIMart Image Gen | GPT-Image-2 异步图片生成 | 批量生图 |
| DeepWhite | 图像提示词SKILL | 提示词工程 |
| Design Image Studio | 设计生图管线 | 设计稿生成 |
| HY 3D Gen | 腾讯混元生3D | 3D生成 |
| Multi-Agent Image | 多Agent图片工作流 | 复杂图像任务 |
| Book Illustration | 书稿截图配图工作流 | 配图管线 |

   匹配逻辑: 指令意图 → 工具适用表 → 选择最适配工具+其提示词写法
3. **提示词结构模式选择** (来自15-方法库统计):
   - 自由描述型(60%) / JSON结构化(15%) / 模板填空(15%) / 中英双语(10%)
   - 高频成功技巧: 锁定比例 / 强制文字 / 平台特征 / 结构化模块 / 负面约束
4. **注入规则**: 方法论只标来源不臆造; 无匹配时标记"无方法论可用"，不得编造

### Token性价比策略

- 简单指令(≤3维信息): 只扫1-2个文件，≤5KB
- 中等指令(3-5维): 扫3-5个文件，≤15KB
- 复杂指令(5+维/跨域): 扫5-10个文件，≤30KB(分批加载)

### 用户指定范围

如果用户在S1.5指定了自定义扫描范围, 优先按用户指定执行:
```
用户指定: "扫描技能库\DEV-成码\ 和 <指令库>\文字-AI提示词-工程\"
→ 忽略默认域扫描策略，按用户指定路径执行
```

---

## S4 化神分析（动态轮数）

**目标**: 对S3扫描到的内容和S0原始指令进行综合分析，提取可用于指令词优化的素材。

### 分析维度

| 维度 | 权重 | 评分标准(1-10) |
|------|------|---------------|
| 可用度 | 25% | 扫描内容中多少可以直接用于指令优化 |
| 关联度 | 20% | 扫描内容与原始指令的相关程度 |
| 完整性 | 20% | 扫描内容覆盖了指令优化的哪些方面 |
| 一致性 | 15% | 提取的内容之间是否协调一致 |
| 价值度 | 10% | 这些内容是否显著提升指令质量 |
| 新颖性 | 10% | 是否有用户不知道的优化方法/模式 |

### 动态轮数规则

| 任务复杂度 | 判定标准 | 化神轮数 | 最高评分要求 |
|-----------|---------|---------|------------|
| 简单 | 单域+≤3个优化点 | 3轮 | 第3轮≥8.0 |
| 中等 | 单域+3-6个优化点 | 5-6轮 | 第5轮≥8.0 |
| 复杂 | 跨域/6+个优化点 | 7-9轮 | 第7轮≥8.0 |
| 极复杂 | 跨域+系统级 | 9-12轮 | 第9轮≥8.0 |

**自动判定**: S2域识别+S0完整度 → 预估任务复杂度 → 确定初始轮数

**收敛条件**:
- 连续2轮composite提升<0.5 → 提前收敛(停止后续轮次)
- 某轮composite≥8.5且提升<0.3 → 提前收敛
- 用户说"够了" → 立即终止

**每轮输出**:
```
[化神 第N轮]
composite: X.X/10
本轮发现: [发现了哪些可利用内容]
主要价值: [对指令优化的具体价值]
当前进度: 已利用 X% 的扫描材料
```

---


### 化神盲点扫描（千面视角）[新增]
**目标**: 借千面（META-角色智库-千面）多专家/多人格视角，发现用户未意识到的关键盲点（影响决策与工作的结构性缺口），而非仅做库内比对。

**触发**: 用户说"化神分析/盲点洞察/我忽略了什么"，或元决 S4 进入"深层"档时自动启用。

**执行**:
1. 调用千面匹配 2-3 个最相关专家人格（如 系统架构师/知识工程师/产品架构师）
2. 各人格从自身视角输出 1 条"用户 unaware 的关键点" + 决策影响
3. 收敛为 ≤3 条高影响盲点，标注"为何用户看不到"（信息孤岛/路径依赖/工具未接）
4. 输出动作指令（指向检索/索引/去重类基建）

**连接**: 千面(角色智库) → 元决(S4) → 落点写入知识库/指令库待办

## S5 输出优化

**目标**: 将化神分析结果转化为优化后的指令词，并输出优化日志。

### 输出格式

```markdown
## 元决优化结果

### 优化后指令词
[根据化神分析整合后的最终指令]

### 优化日志
- 原始指令: [用户输入的原文]
- 完整度评估: [S0评分]
- 问心沟通: [S1轮数 + 补充了哪些信息]
- 域匹配: [S2判定域]
- 扫描范围: [S3扫描了哪些区域]
- 化神分析: [S4轮数 + 最终composite]
- 关键优化点: [哪些内容被利用来优化]
- 输出日期: [时间戳]
```

### 开工指令词存档

如果优化结果属于可复用的开工型指令，同步写入:
```
路径: <指令库>\000-开工指令集\
文件名: {日期}_{指令摘要}.md
格式: 
  - 标题: [指令名称]
  - 触发场景: [什么情况下用]
  - 完整指令: [优化后的指令词]
  - 优化记录: [来源+时间]
```

### 5.5 约束硬化 + 质量门控 (元决强化 2026-08-09)

**S5.5a 约束硬化**: 输出优化后指令词前，逐项锁定:
1. 负面约束: 禁AI句式 / 禁占位(A18) / 禁编造指标(A15)
2. 参数锁定: 比例/格式/平台特征/版本号
3. 防幻觉: 每条具体参数必须有扫描或知识库中的来源引用

**S5.5b 质量门控**: 交付前自检三项:
1. 完整性 — 无占位(A18全检)
2. 可执行性 — 指令可直接复制使用
3. 验证路径 — 指出验证方式或门控

**S5.5c 评分输出**: 每次优化必须输出
```
[优化前得分] X/10 → [优化后得分] Y/10
优化点清单: 1... 2... 3...
可复制指令词: [完整版, 粘贴即用]
```

---

## S6 自升级

**目标**: 将本次优化的经验反馈到元决自身，持续进化。

### 化神分析·重复→SKILL 候选 [新增]

**目标**: 将"化神分析"第 2 问（重复工作识别）固化为 S6 的常驻输入——由千面视角扫描会话/指令库，自动产出候选 SKILL 封装方案，不再依赖用户手动说"沉淀元决"。

**触发**:
- 用户说"分析我的重复工作"/"哪些值得封装SKILL"
- 元决 S6 进入时，先跑一轮化神分析再决定是否沉淀

**执行**:
1. 千面匹配"流程挖掘"类人格，扫描近期会话/指令库高频动作
2. 聚类重复模式 → 输出 ≤3 个候选 SKILL（名称/原因/使用场景/触发词）
3. 与现有技能库查重（INDEX 比对），标记"新建/合并/已有"
4. 用户确认后写入 `技能库/` 对应域目录 + 更新 INDEX

**连接**: 千面(角色智库) → 元决(S6) → 技能库(落库)

### 触发条件
- 完成S5后自动评估: "本次优化是否暴露了元决流程的不足?"
- 连续3次同类型优化 → 沉淀为模板
- 用户说"更新元决" → 执行S6

### 自升级方式
1. 记录优化模式到本SKILL.md的「经验模式库」
2. 更新S0完整度评估的权重(命中率校准)
3. 更新S3扫描策略(发现新的高价值扫描区域)
4. 更新S4化神维度(新增有效维度)

### 经验模式库

| # | 发现时间 | 指令模式 | 优化策略 | 效果 |
|---|---------|---------|---------|------|
| (由S6自动记录) |

---

## 约束

- S1问心最多5轮(含S1.5确认)，超限按当前理解执行
- S4化神最多12轮，超限强制输出
- S6自升级一周最多1次(防过度优化)
- 输出格式必须包含「优化后指令词」和「优化日志」
- 不修改用户原始文件，只输出优化结果

## 焚诀执行纪律（用户Miao偏好）

- **先分析再执行**: 任何系统级任务必须先化神分析/审计，再按优先级顺序执行
- **到渣滓状态**: 不允许留尾巴——任务完成标准是"完成到渣滓状态"(完全彻底，无可再改)
- **深度扫描铁律**: 分析系统时不能只看顶层模块目录，必须递归到子模块级（模式-*.md文件/具体工作流文件）。只看到管线层=分析太浅，用户会说"不对 下一层级还是要扫描"
- **逐路径分析**: 每个功能路径都要独立化神分析，不能只到成图/成文/成影这一层就汇总
- **顺序执行**: 按P0→P1→P2顺序，不跳跃
- **花叔熔锻红线**: 见 `功能-优化协议/花叔仓库熔锻分析.md` — 先读后改、逐文件验证、轮数化神9维评分、创建熔锻文档后才能删源
- **归档验证**: 执行后必须将产出的报告/文件归档到案例库熔锻报告目录，并做最终验证
- **并行子代理**: 大任务用delegate_task并行子代理，各自报结果后汇总

## 参考

| 文件 | 用途 |
|------|------|
| `references/10轮化神-系统审计方法论.md` | 系统级多模块审计(100+模块级),固定10维分析 |
| `references/品鉴标准工作流管线.md` | 品鉴系统5条端到端工作流管线定义 |
| `references/品鉴系统审计与优化执行模式.md` | 品鉴专用审计5阶段管线: 化神十轮→问题归类→并行修复→验证→归档 |
| `references/设计输出-门控执行协议.md` | ★设计输出门控执行协议: 13步顺序/输出格式规范/17必填字段/质感层次5项/场景自适应品类映射 |
| `功能-优化协议/花叔仓库熔锻分析.md` | 花叔(alchaincyf)49仓库熔锻分析启动词+红线+已处理清单 |

## 祛幻层 — 防幻觉门控 (R1-R12 + 变人v2.2)

> 每次S5输出优化前自动执行，不可跳过

### 层1: AI味检测（常驻）
| 维度 | 记分 | 触发条件 |
|------|------|---------|
| 优化指令虚构 | +3/处 | 优化后的指令引用了不存在的文件/路径/资源 |
| S4分析无依据 | +2/处 | 化神评分无扫描材料的原文引用 |
| AI句式 | +0.5/处 | 首先/值得注意的是/总的来说/从这里可以看出 |
| 空洞优化 | +1/处 | 优化建议无具体修改内容(只写"优化表达"之类) |

**门控**: 总分 > 5 → 重写 (迭代≤3轮)

### 层2: 引用溯源强制
每条优化结论必须带扫描来源引用(文件路径+具体内容)

---

## Self-Check

- [ ] 祛幻AI味检测总分≤5？(>5已重写)
- [ ] 优化后指令词中的每条指令都有扫描材料中的来源引用？
- [ ] S0完整度评估使用真实维度分而非虚构？
- [ ] 化神分析的轮次每轮都有独立发现+依据？
- [ ] 优化日志包含了完整(原始指令/完整度/问心/域/扫描/化神/关键点)？
- [ ] 如果S6自升级触发，本次经验是否已记录到经验模式库？

## OM集成 — 多Agent协作记忆

元决的优化历史写入OM，帮助编排器和化神了解哪些指令已被优化。

### 优化完成时写入
```powershell
$summary = "[元决] 优化: {指令简述} | S0: {分数}→优化后 | 轮次: {N}轮 | 关键点: {优化要点} | $(Get-Date -Format 'yyyy-MM-dd')"
$body = @{content=$summary; tags=@("domain:meta-opt", "source:元决", "scope:global")} | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:8080/memory/add" -Method Post -Body $body -ContentType "application/json" -Headers @{"x-api-key"=$env:OM_API_KEY}
```

## 子模块

| 模块 | 功能 |
|------|------|
| 功能-优化协议 | 全域优化执行标准(代码/内容/流程/指令四领域) |
| 桥接引擎 | THINK-沟通-问心桥接 |
